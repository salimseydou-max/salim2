## Smart Advice Generator

This repo contains the **Smart Advice Generator** React app (per the PRD).

### Run locally

```bash
cd smart-advice-generator
npm install
npm run dev
```

### Build

```bash
cd smart-advice-generator
npm run build
npm run preview
```

